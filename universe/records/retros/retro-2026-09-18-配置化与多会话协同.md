# 复盘 004 · 配置化与多会话协同（2026-09-18）

> 触发：用户指令「书库地址、结果目录等通过配置文件配置，首次使用引导、后期可改，skill 不用真实地址」。范围：skill 配置机制 + 本轮盘点暴露的批量模式欠账与索引缺口。

## 一、暴露的问题

| # | 问题 | 证据 |
|---|---|---|
| 1 | **skill 硬编码真实地址 3 处**：SKILL.md 默认根目录、mcp/.mcp.json、server.py 默认宇宙路径 | 逐文件 grep 实锤；换机/迁移即失效 |
| 2 | **批量模式跳过 step 4 产物**：四本批量书全部缺 04-network/related.md | LIBRARY.md ✓✗ 首跑即抓出（索引设计当日验证有效） |
| 3 | **Dream 触发漏报**：经济学域概念 13 个 >12 上限，批量期间零提示 | LIBRARY 概念域索引计数；靠人工盘点才发现 |
| 4 | **并发会话编辑冲突**：本会话编辑 SKILL.md 时另一会话已写入铁律 4 | Edit 报「File has been modified」；重读后合并，无覆盖损失 |
| 5 | 批量书 frontmatter 缺 domain/cost 字段 | 4 本落「未分组」；索引字段表此前未成文 |

## 二、本次升级（配置机制为核心）

**配置文件**：`~/.agents/skills/chaishu/config.yaml`（机器本地，不入 skill 源码分发），两键：`library_root`（书库 PDF 分类树根）、`workspace_root`（拆书结果根）。首跑读不到 → AskUserQuestion 引导两问写盘，不阻塞当次任务；后期直接编辑或口头改。解析顺序：CWD 已初始化工作区优先（防踩错库，提醒回写）> workspace_root > 首跑引导。MCP 与 universe_rebuild.py 同读此文件。

**验证**：无参 rebuild 走 config 成功（11 本/49 概念/87 边）；MCP 启动无配置报错；项目源 grep 真实地址 = 0（真实值只存在于安装目录 config.yaml）。

**连带回写**：SKILL.md（配置节/根目录行/自主权契约/批量 step4 不跳/Dream 判据以 LIBRARY 概念域索引计数为准）；universe.md 书目 frontmatter 字段表（9 字段必填项成文）；rebuild 与 server.py 去 hardcoded 默认值；.mcp.json 转占位模板 + README 注册节重写；config.example.yaml 模板入项目源。

**数据层修复**：4 本批量书 domain 回填（行为科学×2、认知成长·学习科学、法律·法理思维）+ 索引重建；目录清理 6 项（HANDOFF、需求文档 v1/v2、调研报告、边表备份、pycache）。

## 三、遗留

1. 批量 4 本补 related.md（建议塞进 Dream-003 间隙）
2. **Dream-003 未跑且双触发已满足**（拆完下一本即第 12 本 = 4 的倍数；经济学域 13 概念超限）——建议艾希曼拆完立即执行
3. 薛书两欠账（测绘图、学习手册）；人格提案 v11/v12 待裁决
4. 多会话并发回写 skill 无守则——本次靠「Edit 失败→重读→合并」化解；若复发，考虑约定回写前 diff 或单会话回写窗口
